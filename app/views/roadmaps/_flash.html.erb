<% if @roadmap.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@roadmap.errors.count, "error") %> prohibited this post from being saved:</h2>

    <ul>
      <% @roadmap.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>